export const body = document.querySelector("body") as HTMLBodyElement;

export const menu = document.querySelector("menu") as HTMLMenuElement;

export const app = document.querySelector("#app") as Element;

export const content = document.querySelector("#content") as Element;

export const openMenuButton = document.querySelector(
  "#open-menu-button"
) as Element;

export const closeMenuButton = document.querySelector(
  "#close-menu-button"
) as Element;

export const navigationButtons =
  document.querySelectorAll(".navigation-button");

export const pages = document.querySelectorAll(".page");

export const bullets = document.querySelectorAll(
  ".bullet"
) as NodeListOf<HTMLSpanElement>;
